<div class="activity">
  <%= activity.owner.email %> 
  为 <%= link_to activity.trackable.contact.full_name, activity.trackable.contact %>
  创建了新的联系记录：
  <%= truncate(activity.trackable.notes, length: 50) %>
  <span class="activity_time">大约<%= time_ago_in_words(activity.created_at) %>前</span>
</div>
