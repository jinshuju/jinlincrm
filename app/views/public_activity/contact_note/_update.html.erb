<div class="activity">
  <%= activity.owner.email %> 
  为 <%= link_to activity.trackable.contact.full_name, activity.trackable.contact %>
  更新了联系记录
  <span class="activity_time"><%= time_ago_in_words(activity.created_at) %>前</span>
</div>
